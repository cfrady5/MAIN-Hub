# Canonical site URL used for metadata, sitemap and Open Graph tags.
NEXT_PUBLIC_SITE_URL=https://www.example.org

# Where contact-form submissions are forwarded as JSON (Zapier/Make hook, CRM intake, Slack webhook, etc.).
# Leave unset to show the email fallback instead of accepting submissions.
CONTACT_WEBHOOK_URL=
